//app/_utils/frontend/getEndIndex.ts
// 목표 : 배열의 마지막 인덱스를 반환합니다.

/**
 * 배열의 마지막 인덱스를 반환합니다.
 *
 * @param arr 배열
 * @returns 마지막 인덱스
 */
export default function getEndIndex(arr: any[]): number {
  return arr.length - 1;
}
